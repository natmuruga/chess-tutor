fastapi>=0.115
uvicorn[standard]>=0.30
python-chess>=1.10
httpx>=0.27
soundfile>=0.12
numpy>=1.26
# optional, enable as you go:
# kokoro>=0.9            # TTS, Apache 2.0, CPU-friendly (also: pip install misaki[en]; apt install espeak-ng)
# chatterbox-tts         # TTS with voice cloning, MIT, needs a GPU
# faster-whisper>=1.0    # speech to text, MIT
